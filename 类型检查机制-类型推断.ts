// ============基础类型推断==============
let a = 1;
let b = [1];

let c = (x = 1) => x + 1;

// =================上下文的类型推断====================
// 根据左侧的事件绑定推断出右侧的事件类型
window.onkeydown = (event) => {
  console.log(event.button);
};

// ================类型断言=======================
// ts的推断不符合你的预期，或者你完全有自信比ts更了解你的代码
interface Foo {
  bar: number;
}

let foo: Foo = { bar: 1 };
